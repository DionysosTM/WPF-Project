<Window x:Class="projet_wpf.Views.EditTagsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:projet_wpf.Views"
        mc:Ignorable="d"
        Title="EditTagsWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="TagsListBox" DisplayMemberPath="Text" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Ajouter" Width="70" Margin="0,0,5,0" Click="AddTag_Click"/>
            <Button Content="Modifier" Width="70" Margin="0,0,5,0" Click="EditTag_Click"/>
            <Button Content="Supprimer" Width="70" Margin="0,0,5,0" Click="DeleteTag_Click"/>
            <Button Content="Fermer" Width="70" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
